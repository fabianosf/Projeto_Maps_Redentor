# Runtime RedMapa (Flask + DAL + auth)
flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
bcrypt>=4.1.0
cryptography>=42.0.0
pandas>=2.0.0
pymysql>=1.1.0

# SGBD alternativo no DAL (opcional se usar só MariaDB)
psycopg2-binary>=2.9.9

# Testes
pytest>=8.0.0
pytest-cov>=5.0.0

# Opcional — consultas ERP (Oracle) via get_erp_dal_instance / erp.dat
# oracledb>=2.0.0

# Opcional — scripts de documentação/imagens (não necessário para API)
# Pillow>=10.0.0
