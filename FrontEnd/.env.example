# Base URL da API Flask (sem barra final no host; inclua /api/v1).
# Em desenvolvimento o Vite faz proxy de /api → localhost:5000, então o padrão basta:
VITE_API_URL=/api/v1

# Exemplo apontando direto ao BackEnd (build / celular / sem proxy):
# VITE_API_URL=http://192.168.0.10:5000/api/v1
